---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: federatedlearnings.federation-ai.open-cluster-management.io.open-cluster-management.io
spec:
  group: federation-ai.open-cluster-management.io.open-cluster-management.io
  names:
    kind: FederatedLearning
    listKind: FederatedLearningList
    plural: federatedlearnings
    singular: federatedlearning
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FederatedLearning is the Schema for the federatedlearnings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FederatedLearningSpec defines the desired state of FederatedLearning.
            properties:
              client:
                properties:
                  image:
                    type: string
                  placement:
                    properties:
                      name:
                        type: string
                    type: object
                type: object
              framework:
                description: FrameworkSpec is the choosing federated learning framework
                  running in the open cluster management environment
                enum:
                - flower
                - openfl
                - other
                type: string
              server:
                properties:
                  Listeners:
                    items:
                      properties:
                        name:
                          type: string
                        port:
                          type: integer
                        type:
                          type: string
                      type: object
                    type: array
                  image:
                    type: string
                  rounds:
                    type: integer
                type: object
            type: object
          status:
            description: FederatedLearningStatus defines the observed state of FederatedLearning.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
